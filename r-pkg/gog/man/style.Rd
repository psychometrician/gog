% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{style}
\alias{style}
\title{Set constant visual properties on a layer.}
\usage{
style(
  color = NULL,
  opacity = NULL,
  size = NULL,
  shape = NULL,
  border_color = NULL,
  border_size = NULL,
  caps = NULL,
  center = NULL,
  nudge = NULL,
  pattern = NULL,
  arrow = NULL,
  reach = NULL,
  ...
)
}
\arguments{
\item{color}{CSS color name (\code{"steelblue"}) or hex (\code{"#4e79a7"}).}

\item{opacity}{Number in 0–1.  Set literally — unlike the \code{opacity}
channel there is no data range to rescale from.}

\item{size}{Pixels: point radius (default 4.5), or line stroke width
(default 2).}

\item{shape}{One of \code{"circle"}, \code{"square"}, \code{"triangle"},
\code{"diamond"}, \code{"cross"}.}

\item{border_color}{Outline color for a closed glyph.}

\item{border_size}{Outline width for a closed glyph.}

\item{caps}{\code{TRUE} (default) draws the end caps on an interval; \code{FALSE}
leaves it bare.}

\item{center}{\code{TRUE} (default) draws a box plot's median line.}

\item{nudge}{Offset a text label from its point, in pixels.}

\item{pattern}{A stroke's dash pattern, or a fill's hatch tile.}

\item{arrow}{Draw the stroke as an arrow.}

\item{reach}{How far a density curve runs past the data.}

\item{...}{Further settings; an unknown one is refused by name rather
than ignored.}
}
\description{
Channels \emph{map}: \code{color(species)} asks the reader "which species?"
and earns a legend to answer it.  \code{style()} \emph{sets}: it fixes a
visual property at one value for the whole layer, maps no column, consumes
no scale, and produces no legend — there is nothing to decode.
}
\details{
A constant is therefore not a channel, by the same reasoning that keeps
rotation out of the channel set: a channel maps a data column to a visual
feature, and a constant maps nothing.
}
\examples{
\dontrun{
data(df) + x(a) + y(b) + point + style(color = "tomato", opacity = 0.3)
data(df) + x(a) + y(b) + line + style(color = "gray70", size = 1)
}
}
