% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{repel}
\alias{repel}
\title{Move overlapping labels apart until they can be read.}
\usage{
repel
}
\value{
A \code{gog_atom} joined to a mark with \code{*}.
}
\description{
\code{text * repel} reads where the labels and the points actually sit and pushes
each label a different way until they stop overlapping, drawing a thin leader
line back to the point when a label has moved clear of it. Where there is no
arrangement that fits, every label is still drawn and the plot reports how many
are still crowded.
}
\examples{
\dontrun{
# Every country named, with the names moved off one another.
gapminder_2007 <- book_table("gapminder_2007")
p <- data(gapminder_2007) + point + text * repel +
  x(gdp) + y(life) + label(country)
}

}
\seealso{
\code{\link[=style]{style()}}'s \code{nudge}, the constant offset this is the data-derived
counterpart to; \code{\link[=jitter]{jitter()}}, the same idea for a scatter's points.
}
