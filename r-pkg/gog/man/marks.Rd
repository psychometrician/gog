% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{marks}
\alias{marks}
\alias{point}
\alias{line}
\alias{path}
\alias{rule}
\alias{zone}
\alias{area}
\alias{bar}
\alias{step}
\alias{interval}
\alias{ribbon}
\alias{text}
\alias{surface}
\alias{edge}
\title{Marks -- the shapes a plot draws}
\format{
Each is a \code{gog_atom} object, used bare rather than called.
}
\usage{
point

line

path

rule

zone

area

bar

step

interval

ribbon

text

surface

edge
}
\description{
A \strong{mark} is the shape a layer draws, and every plot needs exactly one. A mark
carries no data of its own: it is combined with \strong{channels} that say which
column goes to which part of the picture, and the engine decides every stroke
from there.
}
\details{
Marks are the grammar's consonants. They are plain objects rather than
functions, so they are written bare in a sentence:

\code{data(table) + point + x(column) + y(column)}

A mark and its required positions together are the smallest thing that draws;
neither renders alone. Which channels a mark accepts is a property of the mark,
and asking for one it cannot carry is refused with a direction rather than
silently ignored.
}
\section{The marks}{

\describe{
\item{\code{point}}{One glyph per row. How two measurements move together.}
\item{\code{line}}{Connects rows in \strong{x order}. How one value changed over time.}
\item{\code{path}}{Connects rows in \strong{data order} rather than x order, so a series
may double back on itself.}
\item{\code{area}}{Fills the region between the line and the baseline.}
\item{\code{bar}}{A rectangle from the baseline to the value. How large each
category is.}
\item{\code{step}}{The line family's right-angled member: a value holds steady
until it jumps.}
\item{\code{interval}}{A segment from a low value to a high one. Needs a pair, so
it is usually written with \code{range} or \code{confidence}.}
\item{\code{ribbon}}{A band between two values across a span.}
\item{\code{text}}{Draws the string given by \code{label} at each position.}
\item{\code{rule}}{A reference line across the panel -- the threshold everything
else is compared against.}
\item{\code{zone}}{Shades a rectangular region of the panel.}
\item{\code{surface}}{A sheet over a grid of samples, for three-dimensional data.}
}

\code{box} is a mark too, but takes an argument, so it is documented separately.
}

\examples{
# A mark plus its positions is the smallest sentence that draws.
p <- data(mtcars) + point + x(wt) + y(mpg)

# The same data, a different shape: one atom is all that changes.
q <- data(mtcars) + line + x(wt) + y(mpg)

}
\seealso{
\code{\link[=box]{box()}} for the box-and-whisker mark; \code{\link[=x]{x()}} and \code{\link[=y]{y()}} for positions;
\code{\link[=style]{style()}} for settings that do not vary with the data.
}
