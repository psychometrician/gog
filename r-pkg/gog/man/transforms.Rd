% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{transforms}
\alias{transforms}
\alias{smooth}
\alias{count}
\alias{sum}
\alias{mean}
\alias{median}
\alias{max}
\alias{min}
\alias{proportion}
\alias{range}
\title{Transforms -- statistics computed before the mark is drawn}
\format{
Each is a \code{gog_atom} object, used bare rather than called.
}
\usage{
smooth

count

sum

mean

median

max

min

proportion

range
}
\description{
A \strong{transform} replaces the rows with a summary of them, and the mark then
draws that summary. It is joined to a mark with \code{*}, which reads as
derivation: \code{bar * count} is a bar of counts, and it is where a histogram
comes from rather than being a chart type to look up.
}
\details{
The same transform behaves identically on every mark it is legal on. Keep the
statistic and change the shape and the same numbers draw a line, an area or a
step -- that is the point of separating the two.
}
\section{The transforms}{

\describe{
\item{\code{count}}{The number of rows for each value of the grouping position.}
\item{\code{proportion}}{Each group's share of the total, rather than its count.}
\item{\code{sum}, \code{mean}, \code{median}, \code{max}, \code{min}}{The aggregate of the measure
within each group.}
\item{\code{range}}{The lowest and highest value in each group, as a pair -- which
is what \code{interval} and \code{ribbon} need.}
\item{\code{smooth}}{A fitted trend through the points rather than the points.}
}

Transforms that take an argument are documented on their own pages:
\code{\link[=bin]{bin()}}, \code{\link[=density]{density()}}, \code{\link[=confidence]{confidence()}}, \code{\link[=bounds]{bounds()}}, \code{\link[=partition]{partition()}}.
\code{\link[=stack]{stack()}}, \link{dodge} and \code{\link[=jitter]{jitter()}} are a different kind of atom -- they resolve
the \emph{overlap} of marks rather than computing a statistic.
}

\examples{
# A histogram is a bar of binned counts -- derived, not a chart type.
h <- data(mtcars) + bar * count + x(cyl)

# The same statistic, a different shape.
l <- data(mtcars) + line * count + x(cyl)

# An aggregate of a measure within each group.
m <- data(mtcars) + bar * mean + x(cyl) + y(mpg)

}
\seealso{
\link{marks} for the shapes these are drawn with; \code{\link[=stack]{stack()}} for resolving
overlap.
}
