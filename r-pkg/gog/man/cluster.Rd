% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{cluster}
\alias{cluster}
\title{Join the closest leaves under one elbow -- the cluster tree.}
\usage{
cluster(value, over)
}
\arguments{
\item{value}{The numeric column distances are measured on, bare.  Omit it on
a tile plot, where \code{color()} already names the values.}

\item{over}{The profile column, bare: each leaf holds one value at every
level of it.  Omit it only when the table has one row per leaf.}
}
\description{
The leaves are the levels of the bound categorical position, and each one is
described by a profile: its \code{value} at every level of the \code{over} column.
The engine measures the distance between every pair of profiles, joins the
closest two, and keeps joining until one tree holds them all.  The other
position stays unbound and carries the merge distance, which the axis names
itself.
}
\details{
Two marks read the one tree.  \code{path * cluster(amount, over = nutrient) + x(food)} draws it: each elbow joins two branches at the height they merged,
so the reader can see which leaves sit close and where the groups part.
\code{y(food)} instead lays the same tree on its side.  \code{zone * cluster(over = nutrient)} is the other reading: the tile plot unchanged, with the leaf
axis's slots reordered to the tree's leaf order, so alike rows sit together.
On a tile plot the values are already on \code{color}, which is why \code{value} is
omitted there.

Composed with \code{|} and \code{/}, a clustered panel decides the order of any axis
it shares, which is what puts a tree flush against a reordered tile plot.

The statistic is fixed: distances are Euclidean on the values as given, and
branches join at their average distance.  Rescale the value column where the
data lives if its units should not weigh in.
}
