% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atoms.R
\name{globe}
\alias{globe}
\title{View the sphere itself.}
\usage{
globe(turn = 0, tilt = 0)
}
\arguments{
\item{turn}{Degrees of longitude the view faces. Default 0.}

\item{tilt}{Degrees of latitude the view faces, -90 to 90. Default 0.}
}
\description{
The globe coordinate space. \code{x} is \strong{longitude} and \code{y} is \strong{latitude},
both in degrees, and each mark stands at its place on the sphere's surface.
The marks that draw on a \code{\link[=map]{map()}} draw here: \code{point} is a place, \code{path} is a
route bending along great circles, \code{text} is a name at a place, \code{rule} holds
one coordinate whole (a meridian pole to pole, or a parallel all the way
round), and \code{zone} with \code{group()} fills each region of a boundary. The
graticule is the panel grid, turned off like any other with
\code{theme(grid = "none")}.
}
\details{
A globe shows one half of the earth. \code{turn} and \code{tilt} name the place the
view faces, in the words \code{space()} already uses: \code{globe(turn = 178, tilt = -18)} puts Fiji at the center of the disk. A bearing wraps, so any
\code{turn} is a view; \code{tilt} is a latitude and stops at the poles. Rows on the
far half are hidden behind the sphere, and gog says how many rather than
dropping them in silence. In the web edition, dragging the globe turns it.

Both positions are spent on the place, so a mark that measures along an axis
has none left; carry a quantity on \code{size()} or \code{color()} instead, as on a
map. A binned field on the sphere is designed and not drawn yet: its correct
tiling is hexagonal, because rectangular bins do not cover equal area on a
sphere, and that equal-area grid is not built.
}
\examples{
\dontrun{
data(quakes_fiji) + point + x(east) + y(north) + size(magnitude) +
  globe(turn = 178, tilt = -18)
data(borders) + zone + x(lon) + y(lat) + group(country) +
  color(continent) + globe(turn = 20, tilt = 5)
}
}
