% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{gog_table}
\alias{gog_table}
\title{Read one of the book's example tables}
\usage{
gog_table(name, text = character())
}
\arguments{
\item{name}{The table's name without the extension, such as
\code{"gapminder_2007"}.}

\item{text}{Columns that must stay text. A CSV records what a value is and
never what kind of thing it is, so a column of \code{01}, \code{02}, \code{03} comes back
as the numbers 1, 2, 3 unless it is named here.}
}
\value{
A data frame.
}
\description{
Fetches a table published beside the manual and returns it ready to plot.
The full list of names is in the book's data chapter.
}
\examples{
\dontrun{
gapminder_2007 <- gog_table("gapminder_2007")
data(gapminder_2007) + point + x(gdp) + y(life)

# `session` holds labels that look like numbers, so it stays text.
sessions <- gog_table("sessions", text = "session")
}
}
