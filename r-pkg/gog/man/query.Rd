% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{query}
\alias{query}
\title{A table that lives in a database}
\usage{
query(con, sql, name = NULL)
}
\arguments{
\item{con}{A DBI connection.}

\item{sql}{A single \code{SELECT}, as a string.}

\item{name}{What to call the table. Defaults to \code{"query"}; a second query in
one sentence needs its own name, since a layer resolves its bare columns
against the nearest table \emph{by name}.}
}
\value{
A \code{gog_spec}.
}
\description{
\code{query()} stands exactly where \code{\link[=data]{data()}} stands, and \strong{nothing after it
changes} — the same operators, channels, bare column names and transforms:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{data(orders)                             + bar + x(status)
query(con, "SELECT * FROM orders")       + bar + x(status)
}\if{html}{\out{</div>}}

The SQL is confined to this one argument and never enters the grammar, which
is the point: \code{x(status)} is still a bare column resolved by the same mask,
not a fragment of another language.

The connection is the caller's own — gog opens none. It is a \strong{DBI}
connection, R's database standard, so RSQLite, RPostgres, RMariaDB, odbc and
bigrquery all reach this. DBI is Suggests rather than Imports: a user who
never writes SQL should not install a database stack to draw a plot.

The query is \strong{not run here}. It runs once, at render.
}
